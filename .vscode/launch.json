{
  "configurations": [
   {
     "type": "node",
     "request": "launch",
     "name": "Debug Jest for current test file",
     "program": "${workspaceFolder}/node_modules/jest/bin/jest",
     "args": ["test", "--runInBand", "--verbose", "false", "${fileBasename}"],
     "cwd": "${workspaceFolder}"
   }
  ]
}